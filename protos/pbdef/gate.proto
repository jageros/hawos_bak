syntax = "proto3";

package pbf;

service Gate {
  rpc GetGateInfo (DefaultArg) returns (GateInfo) {}
}

message DefaultArg {}

message GateInfo {
  string Scheme = 1;
  string Host = 2;
  string Path = 3;
}
